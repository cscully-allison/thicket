import { select, selectAll } from 'd3-selection';

import {
  scaleLinear
} from 'd3-scale';

import { axisBottom } from 'd3-axis';

import { json } from 'd3-fetch';

import { quantile, bin } from 'd3-array';

import { path } from 'd3-path';

export default {
  select,
  selectAll,
  scaleLinear,
  axisBottom,
  json,
  quantile,
  bin,
  path
};